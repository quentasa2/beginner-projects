class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:

        x=0
        for x in range(len(nums)):
            y=target-nums[x]
            for a in range(len(nums)):
                if a==x:
                    pass
                elif y==nums[a]:
                    return x,a
