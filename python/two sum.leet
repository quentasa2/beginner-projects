class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:

        x=0
        for x in range(len(nums)):
            for n in range(len(nums)):
                if n==x:
                    pass
                elif nums[n]+nums[x]==target:
                    return n,x
